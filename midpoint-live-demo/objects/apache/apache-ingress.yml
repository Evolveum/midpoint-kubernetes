apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apache-ingress
  namespace: mp-demo
spec:
  tls:
    - hosts:
        - demo.example.com
      secretName: cert-mp-demo
  rules:
    - host: demo.example.com
      http:
        paths:
          - path: /library
            pathType: Prefix
            backend:
              service:
                name: apache-service
                port:
                  number: 80

